apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ./

patches:
  - target:
      kind: HorizontalPodAutoscaler
      labelSelector: name=example-app
    path: patches/hpa-patches.yaml
